#		Mozilla 1.8 Language Pack Configuration
#
# charset of this file must be UTF-8 (need not to be escaped)
# ${locale}, ${targetapp}, ${targetapp}.${locale} prefix will be trimed
# e.g. ja.property.name will be loaded as property.name property
#

#		diff targets configurations

diff-jar.fromlocale		= ${locale}
diff-jar.tolocale		= ${locale}
diff-jar.fromrevision		= ${package.revesion.pre}
diff-jar.torevision		= ${package.revision}

diff-jar.fromfile		= ${pack.dest.dir}/${diff-jar.fromrevision}/${diff-jar.fromlocale}/chrome/${diff-jar.fromlocale}.jar
diff-jar.tofile			= ${pack.dest.dir}/${diff-jar.torevision}/${diff-jar.tolocale}/chrome/${diff-jar.tolocale}.jar
diff-jar.output			= ${diff-jar.dir}/${locale}-${diff-jar.fromrevision}+${diff-jar.torevision}.diff

diff-xpi.fromlocale		= ${locale}
diff-xpi.tolocale		= ${locale}
diff-xpi.fromrevision		= ${package.revesion.pre}
diff-xpi.torevision		= ${package.revision}
diff-xpi.fromfxversion		= ${targetapp.version}
diff-xpi.tofxversion		= ${targetapp.version}
diff-xpi.fromtbversion		= ${targetapp.version}
diff-xpi.totbversion		= ${targetapp.version}
diff-xpi.fromsbversion		= ${targetapp.version}
diff-xpi.tosbversion		= ${targetapp.version}

firefox#diff-xpi.fromfile	= ${pack.dest.dir}/firefox-${diff-xpi.fromfxversion}.${diff-xpi.fromlocale}.langpack-${diff-xpi.fromrevision}.xpi
firefox#diff-xpi.tofile		= ${pack.dest.dir}/firefox-${diff-xpi.tofxversion}.${diff-xpi.tolocale}.langpack-${diff-xpi.torevision}.xpi
thunderbird#diff-xpi.fromfile	= ${pack.dest.dir}/thunderbird-${diff-xpi.fromtbversion}.${diff-xpi.fromlocale}.langpack-${diff-xpi.fromrevision}.xpi
thunderbird#diff-xpi.tofile	= ${pack.dest.dir}/thunderbird-${diff-xpi.totbversion}.${diff-xpi.tolocale}.langpack-${diff-xpi.torevision}.xpi
sunbird#diff-xpi.fromfile	= ${pack.dest.dir}/sunbird-${diff-xpi.fromsbversion}.${diff-xpi.fromlocale}.langpack-${diff-xpi.fromrevision}.xpi
sunbird#diff-xpi.tofile		= ${pack.dest.dir}/sunbird-${diff-xpi.tosbversion}.${diff-xpi.tolocale}.langpack-${diff-xpi.torevision}.xpi
diff-xpi.output			= ${diff-xpi.dir}/${locale}-${diff-xpi.fromrevision}+${diff-xpi.torevision}.diff

diff-xpi.jar.fromfile		= ${diff-xpi.fromfile}/chrome/${diff-xpi.fromlocale}.jar
diff-xpi.jar.tofile		= ${diff-xpi.tofile}/chrome/${diff-xpi.tolocale}.jar
diff-xpi.jar.output		= ${diff-xpi.jar.dir}/${locale}-${diff-xpi.fromrevision}+${diff-xpi.torevision}.diff


#		lpdiff targets configurations

lpdiff-jar.fromlocale		= ${locale}
lpdiff-jar.tolocale		= ${locale}
lpdiff-jar.fromrevision		= ${package.revesion.pre}
lpdiff-jar.torevision		= ${package.revision}

lpdiff-jar.fromfile		= ${pack.dest.dir}/${lpdiff-jar.fromrevision}/${lpdiff-jar.fromlocale}/chrome/${lpdiff-jar.fromlocale}.jar
lpdiff-jar.tofile		= ${pack.dest.dir}/${lpdiff-jar.torevision}/${lpdiff-jar.tolocale}/chrome/${lpdiff-jar.tolocale}.jar
lpdiff-jar.output		= ${lpdiff-jar.dir}/${locale}-${lpdiff-jar.fromrevision}+${lpdiff-jar.torevision}.html

lpdiff-xpi.fromlocale		= ${locale}
lpdiff-xpi.tolocale		= ${locale}
lpdiff-xpi.fromrevision		= ${package.revesion.pre}
lpdiff-xpi.torevision		= ${package.revision}
lpdiff-xpi.fromfxversion	= ${targetapp.version}
lpdiff-xpi.tofxversion		= ${targetapp.version}
lpdiff-xpi.fromtbversion	= ${targetapp.version}
lpdiff-xpi.totbversion		= ${targetapp.version}
lpdiff-xpi.fromsbversion	= ${targetapp.version}
lpdiff-xpi.tosbversion		= ${targetapp.version}

firefox#lpdiff-xpi.fromfile	= ${pack.dest.dir}/firefox-${lpdiff-xpi.fromfxversion}.${lpdiff-xpi.fromlocale}.langpack-${lpdiff-xpi.fromrevision}.xpi
firefox#lpdiff-xpi.tofile	= ${pack.dest.dir}/firefox-${lpdiff-xpi.tofxversion}.${lpdiff-xpi.tolocale}.langpack-${lpdiff-xpi.torevision}.xpi
thunderbird#lpdiff-xpi.fromfile	= ${pack.dest.dir}/thunderbird-${lpdiff-xpi.fromtbversion}.${lpdiff-xpi.fromlocale}.langpack-${lpdiff-xpi.fromrevision}.xpi
thunderbird#lpdiff-xpi.tofile	= ${pack.dest.dir}/thunderbird-${lpdiff-xpi.totbversion}.${lpdiff-xpi.tolocale}.langpack-${lpdiff-xpi.torevision}.xpi
sunbird#lpdiff-xpi.fromfile	= ${pack.dest.dir}/sunbird-${lpdiff-xpi.fromsbversion}.${lpdiff-xpi.fromlocale}.langpack-${lpdiff-xpi.fromrevision}.xpi
sunbird#lpdiff-xpi.tofile	= ${pack.dest.dir}/sunbird-${lpdiff-xpi.tosbversion}.${lpdiff-xpi.tolocale}.langpack-${lpdiff-xpi.torevision}.xpi

lpdiff-xpi.jar.fromfile		= ${lpdiff-xpi.fromfile}/chrome/${lpdiff-xpi.fromlocale}.jar
lpdiff-xpi.jar.tofile		= ${lpdiff-xpi.tofile}/chrome/${lpdiff-xpi.tolocale}.jar
lpdiff-xpi.jar.output		= ${lpdiff-xpi.dir}/${locale}-${lpdiff-xpi.fromrevision}+${lpdiff-xpi.torevision}.html



config.diff.loaded		= TRUE
